{% extends 'base.html.twig' %}

{% block body %}
    <h1>TagNews</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tagNews.id }}</td>
            </tr>
            <tr>
                <th>Idtag</th>
                <td>{{ tagNews.idTag }}</td>
            </tr>
            <tr>
                <th>Idnews</th>
                <td>{{ tagNews.idNews }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tagnews_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tagnews_edit', { 'id': tagNews.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
